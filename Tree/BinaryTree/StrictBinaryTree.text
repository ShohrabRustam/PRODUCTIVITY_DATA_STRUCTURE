Strict Binary Tree:
                The tree have degree 2 or zero know as strict binary tree.

Min node n = 2*h + 1; => m*h + 1;

max node n = 2^h+1 -1 => m^h+1 -1;

Min height h = log2(n+1) -1; => logm(n(m-1)) -1;

max height h = n-1/2 => n-1/m